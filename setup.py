from setuptools import setup, find_packages

setup(
    name='bezier_curve_generator',
    version='0.1.0',
    packages=find_packages(include=['bezier_curve_generator', 'bezier_curve_generator.*'])
)